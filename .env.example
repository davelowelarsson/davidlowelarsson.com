# Copy to .env for local development.
# SHOW_DRAFTS=true renders draft posts (also used by preview deployments).
SHOW_DRAFTS=true

# Cloudflare Web Analytics site token. Optional — leave unset locally and in
# CI; the beacon in src/layouts/Base.astro only renders when this is set.
# CLOUDFLARE_ANALYTICS_TOKEN=
